// src/types.ts

// Interfaz para definir la estructura de un objeto Pelicula
export interface Pelicula {
  id: number;
  title: string;
  genre: string;
  director: string;
  releaseYear: number;
  posterUrl: string;
  }